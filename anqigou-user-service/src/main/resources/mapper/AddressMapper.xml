<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.anqigou.user.mapper.AddressMapper">
    
    <!-- 定义地址结果映射 -->
    <resultMap id="AddressResultMap" type="com.anqigou.user.entity.Address">
        <id property="id" column="id" />
        <result property="userId" column="user_id" />
        <result property="receiverName" column="receiver_name" />
        <result property="receiverPhone" column="receiver_phone" />
        <result property="province" column="province_name" />
        <result property="city" column="city_name" />
        <result property="district" column="district_name" />
        <result property="detailAddress" column="detail_address" />
        <result property="isDefault" column="is_default" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
    </resultMap>
    
    <!-- 查询用户的所有地址 -->
    <select id="selectByUserId" parameterType="String" resultMap="AddressResultMap">
        SELECT * FROM user_address WHERE user_id = #{userId} ORDER BY is_default DESC, update_time DESC
    </select>
    
    <!-- 查询用户的默认地址 -->
    <select id="selectDefaultByUserId" parameterType="String" resultMap="AddressResultMap">
        SELECT * FROM user_address WHERE user_id = #{userId} AND is_default = 1
    </select>
</mapper>